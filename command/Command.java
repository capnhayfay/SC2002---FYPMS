package command;

/**
 * 
 * The Command interface provides a common method signature for all commands in
 * the system.
 */
public interface Command {
    /**
     * Executes the command.
     */
    void execute();
}
